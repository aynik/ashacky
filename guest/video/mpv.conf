# Ashacky: host VideoToolbox decoding, with software fallback.
hwdec=vaapi-copy
# This VM's tested presentation path; gpu-next stalled during 4K60 testing.
vo=gpu
gpu-context=wayland
vd-lavc-threads=4
